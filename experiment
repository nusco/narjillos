#!/bin/bash

# ensure that the code is up to date
./gradlew build

# start an experiment (passing the seed if any)
java -cp core/build/libs/narjillos-0.0.1.jar org.nusco.narjillos.Experiment `git rev-parse HEAD` $1
